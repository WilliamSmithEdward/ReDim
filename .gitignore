__pycache__/
*.pyc
.pytest_cache/
.venv/
~$*
*.tmp
build/
tests/output/
.claude/
# Built workbooks are release assets, not sources: they are zip archives,
# so every rebuild would add a fresh incompressible copy to history.
demo/*.xlsm
